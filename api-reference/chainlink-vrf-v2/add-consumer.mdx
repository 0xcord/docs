---
title: Add Consumer
description: API endpoint for adding a consumer to the Chainlink VRF V2 subscription
api: "POST https://0xcord.com/api/vrfv2/addConsumer"
---

## Params

<ParamField query="dappId" type="integer" required>
  The ID of the dapp to which the consumer will be linked
</ParamField>

<ParamField query="network" type="string" required>
  The blockchain network where the consumer will be linked (e.g., polygon_mumbai, mainnet)
</ParamField>

<ParamField query="consumer" type="string" required>
  The address of the consumer contract to be linked
</ParamField>

## Response

<ResponseField name="data" type="object">
  <ResponseField name="success" type="boolean">
    Indicates whether the consumer was successfully linked
  </ResponseField>
  <ResponseField name="transactionHash" type="string">
    The transaction hash of the linking transaction
  </ResponseField>
</ResponseField>

<Card
  title="Chainlink VRF V2 Tutorial"
  href="/infrastructure/chainlink-vrf-v2"
>
  Learn how to integrate Chainlink VRF V2 with 0xCord
</Card>
